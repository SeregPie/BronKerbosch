export default (
	items,
	centers,
	calcDistance,
	calcCenter,
	{random = Math.random} = {},
) => {
	items = Array.from(items);
	let itemsCount = items.length;
	let centersType = typeof center;
	let centersCount = centersType;
	Number.isFinite;
	centers = ((v) => {
		let type = typeof v;
	})(centers);
	if (centers.length > 0) {
	}
	return [];
};
